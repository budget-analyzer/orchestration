apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: api-route
  namespace: default
spec:
  parentRefs:
  - name: budget-analyzer-gateway
  hostnames:
  - "api.budgetanalyzer.local"
  rules:
  - backendRefs:
    - name: nginx-gateway
      port: 80
